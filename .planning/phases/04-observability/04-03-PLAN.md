---
phase: 04-observability
plan: 03
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - internal/middleware/otel_interceptor.go
  - cmd/app/main.go
  - cmd/sidecar/main.go
autonomous: true
requirements:
  - OBSV-02
must_haves:
  truths:
    - "Trace context propagates automatically across the gRPC boundary between app and sidecar."
  artifacts:
    - path: "internal/middleware/otel_interceptor.go"
      provides: "gRPC interceptors for trace propagation"
  key_links:
    - from: "internal/middleware/otel_interceptor.go"
      to: "grpc server init"
      via: "UnaryInterceptor"
      pattern: "otelgrpc.UnaryServerInterceptor"
---

<objective>
Add OpenTelemetry gRPC interceptors to both the main app and the sidecar so that trace context is propagated end-to-end.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement interceptor wrapper</name>
  <files>internal/middleware/otel_interceptor.go</files>
  <action>
    • Create a package `middleware` with:
      ```go
      func UnaryServerInterceptor() grpc.UnaryServerInterceptor {
          return otelgrpc.UnaryServerInterceptor()
      }
      func StreamServerInterceptor() grpc.StreamServerInterceptor {
          return otelgrpc.StreamServerInterceptor()
      }
      ```
    • Import `go.opentelemetry.io/contrib/instrumentation/google.golang.org/grpc/otelgrpc`.
  </action>
  <verify>
    <automated>go build ./internal/middleware</automated>
  </verify>
  <done>Package builds and returns the correct interceptor types.</done>
</task>

<task type="auto">
  <name>Task 2: Wire interceptor into app gRPC server</name>
  <files>cmd/app/main.go</files>
  <action>
    • Locate the `grpc.NewServer(` call.
    • Add `grpc.UnaryInterceptor(middleware.UnaryServerInterceptor())`, `grpc.StreamInterceptor(middleware.StreamServerInterceptor())`.
    • Ensure import path `internal/middleware`.
  </action>
  <verify>
    <automated>go build ./cmd/app && go run ./cmd/app & sleep 2; curl -s http://localhost:8080/healthz | grep OK</automated>
  </verify>
  <done>App server starts with interceptors attached; a sample request creates a child span in Jaeger.</done>
</task>

<task type="auto">
  <name>Task 3: Wire interceptor into sidecar gRPC server</name>
  <files>cmd/sidecar/main.go</files>
  <action>
    • Apply the same interceptor flags as in Task 2 to the sidecar server construction.
  </action>
  <verify>
    <automated>go build ./cmd/sidecar && go run ./cmd/sidecar & sleep 2; curl -s http://localhost:50051/healthz | grep SERVING</automated>
  </verify>
  <done>Sidecar server starts with interceptors; a request from the app creates a single trace spanning both services.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|------------|-------------|------------|
| T-04-09 | Spoofing | gRPC metadata | mitigate | Interceptors enforce `TraceContext` propagation; mTLS already blocks MITM. |
</threat_model>

<verification>
- Execute a test RPC from the app to the sidecar (`grpcurl` or a Go client). Verify a single trace with parent/child spans appears in Jaeger.
</verification>

<success_criteria>
- Jaeger UI shows a trace containing two spans: one from app, one from sidecar, correctly parent-child linked.
</success_criteria>
