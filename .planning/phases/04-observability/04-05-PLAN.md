---
phase: 04-observability
plan: 05
type: execute
wave: 3
depends_on: - 04
files_modified:
  - test/performance/ghz_benchmark.sh
  - .github/workflows/observability-perf.yml
autonomous: true
requirements:
  - TEST-05
must_haves:
  truths:
    - "Performance benchmark runs automatically in CI and fails if sidecar latency > 5 ms p95 or throughput < 1000 rps."
  artifacts:
    - path: "test/performance/ghz_benchmark.sh"
      provides: "Shell script that runs ghz against sidecar RPCs and asserts latency/throughput thresholds."
    - path: ".github/workflows/observability-perf.yml"
      provides: "GitHub Actions workflow that executes the benchmark on each PR."
key_links:
  - from: "ghz_benchmark.sh"
    to: "sidecar RPC endpoint"
    via: "grpc://localhost:50051"
    pattern: "ghz --insecure"
---

<objective>
Automate a performance test using **ghz** to verify that the sidecar meets the latency and throughput targets specified in the success criteria.
</objective>

<execution_context>
@C:\Repo\cell-arch\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@C:\Repo\cell-arch\.planning\phases\04-observability\04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ghz benchmark script</name>
  <files>test/performance/ghz_benchmark.sh</files>
  <action>
    • Create a shell script that:
      1. Builds the sidecar binary (`go build -o sidecar ./cmd/sidecar`).
      2. Starts the sidecar in the background on a random free port.
      3. Waits for the health endpoint (`/healthz`) to return 200.
      4. Runs `ghz` against the `GetItem` RPC with:
        - `-n 100000` total requests
        - `-c 100` concurrent connections
        - `--latency` flag to capture latency distribution.
      5. Parse the JSON output; fail (`exit 1`) if `p95` latency > 5 ms or `rps` < 1000.
      6. Kill the sidecar process after the test.
  </action>
  <verify>
    <automated>bash test/performance/ghz_benchmark.sh --dry-run</automated>
  </verify>
  <done>Script runs without errors in dry‑run mode and prints the intended ghz command.</done>
</task>

<task type="auto">
  <name>Task 2: Add GitHub Actions CI workflow</name>
  <files>.github/workflows/observability-perf.yml</files>
  <action>
    • Create a workflow triggered on `pull_request` and `push` to `main`.
    • Define two jobs:
      - `setup`: installs Go, installs `ghz` (`go install github.com/bojand/ghz/cmd/ghz@latest`), caches Go modules.
      - `benchmark`: runs `test/performance/ghz_benchmark.sh`.
    • Fail the job if the benchmark script exits with a non‑zero status.
    • Cache Go modules for speed.
  </action>
  <verify>
    <automated>act -j benchmark -W .github/workflows/observability-perf.yml --dry-run</automated>
  </verify>
  <done>Workflow YAML passes `act` dry‑run validation.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-----------|----------|
| T-04-12 | Integrity | Benchmark script | mitigate | Script is version‑controlled; CI runs on a clean workspace. |
| T-04-13 | Availability | CI runner resource exhaustion | mitigate | Limit ghz concurrency to 100 and set a 5‑minute timeout. |
</threat_model>

<verification>
- Run `bash test/performance/ghz_benchmark.sh --dry-run` locally; it should exit 0 and print the intended `ghz` command.
- Push a test branch; GitHub Actions should report the `observability-perf` job as passing.
</verification>

<success_criteria>
- CI job fails if sidecar p95 latency > 5 ms or throughput < 1000 rps.
- Successful runs are reported as a passing check on the PR.
</success_criteria>

<output>
Creates `.planning/phases/04-observability/04-05-PLAN.md`.
</output>