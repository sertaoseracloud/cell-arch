---
phase: "05-ci-cd-security"
plan: "04"
type: execute
wave: 2
depends_on: ["05-01-PLAN"]
files_modified:
  - ".github/workflows/branch-protection.yml"
requirements: ["CICD-05","SECR-02"]
autonomous: true
must_haves:
  truths:
    - "Main branch is protected; direct pushes are rejected"
  artifacts:
    - path: ".github/workflows/branch-protection.yml"
      provides: "GitHub API script for branch protection"
  key_links:
    - from: "branch-protection.yml"
      to: "GitHub API"
      via: "gh api"
---

<objective>
Configure GitHub branch protection for `main` (require PR, status checks, OIDC).  
Purpose: Enforce D-13 (no direct commits) and D-14 (status-check gating).  
Output: `.github/workflows/branch-protection.yml`
</objective>

<execution_context>
@C:\Repo\cell-arch\.claude\get-shit-done\workflows\execute-plan.md
@C:\Repo\cell-arch\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repo\cell-arch\.planning\PROJECT.md
@C:\Repo\cell-arch\.planning\REQUIREMENTS.md
@C:\Repo\cell-arch\.planning\phases\05-ci-cd-security\05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflow that applies branch-protection via GitHub API</name>
  <files>.github/workflows/branch-protection.yml</files>
  <action>
    Define a workflow that runs on repository_dispatch (`workflow_dispatch`) and on `push` to `main` (to self-heal).  
    Steps:
    1. `actions/checkout@v3`
    2. `actions/setup-node@v3` (for `gh` CLI)
    3. Use `gh auth login --with-token ${{ secrets.GITHUB_TOKEN }}` (OIDC-derived token) – no static secret.
    4. Call `gh api repos/${{ github.repository }}/branches/main/protection` with JSON:
       ```json
       {
         "required_status_checks": {"strict": true, "contexts": ["ci", "scan", "integration-test"]},
         "enforce_admins": true,
         "required_pull_request_reviews": {"dismiss_stale_reviews": true},
         "restrictions": null
       }
       ```
    5. Commit the workflow file to the repo (`git add`/`git commit` optional step not required – the file itself is the artifact).  
    Ensure the workflow is protected by the same branch-protection settings it configures (idempotent).
  </action>
  <verify>
    <automated>git diff --exit-code .github/workflows/branch-protection.yml</automated>
  </verify>
  <done>Workflow file exists and can be manually dispatched without errors.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| CI → GitHub API | Uses OIDC token; must not expose PAT. |
## STRIDE
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-08 | Spoofing | OIDC token misuse | mitigate | Scope token to `repo` and `admin:repo_hook` only. |
| T-05-09 | Tampering | Protection payload | mitigate | Validate JSON schema before sending. |
</threat_model>

<verification>
Dispatch the workflow via `gh workflow run branch-protection.yml`.  
Verify via GitHub UI or API that the `main` branch now shows "Require pull request reviews before merging" and required status checks.  
</verification>

<success_criteria>
Main branch cannot be pushed to directly; PRs must pass all three CI jobs.  
</success_criteria>

<output>
Create `.planning/phases/05-ci-cd-security/05-04-PLAN.md`
</output>
