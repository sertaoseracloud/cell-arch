---
phase: "05-ci-cd-security"
plan: "05"
type: execute
wave: 3
depends_on: ["05-01-PLAN","05-02-PLAN"]
files_modified:
  - "infra/secret-store/secretstore-deployment.yaml"
  - "infra/cert-manager/clusterissuer.yaml"
requirements: ["SECR-01","SECR-02","SECR-03"]
autonomous: true
must_haves:
  truths:
    - "CSI Secret Store Driver is deployed in both clusters"
    - "cert-manager provides TLS certs for sidecar services"
  artifacts:
    - path: "infra/secret-store/secretstore-deployment.yaml"
      provides: "K8s deployment for CSI driver"
    - path: "infra/cert-manager/clusterissuer.yaml"
      provides: "ClusterIssuer for both clouds"
  key_links:
    - from: "secretstore-deployment.yaml"
      to: "AWS Secrets Manager / Azure Key Vault"
      via: "CSI provider"
    - from: "clusterissuer.yaml"
      to: "cert-manager controller"
      via: "ACME/CA configuration"
---

<objective>
Deploy the CSI Secret Store driver and cert-manager configuration required for runtime secrets (D-17).  
Purpose: Ensure sidecar can fetch cloud credentials without static secrets.  
Output: Two Kubernetes manifests under `infra/`.
</objective>

<execution_context>
@C:\Repo\cell-arch\.claude\get-shit-done\workflows\execute-plan.md
@C:\Repo\cell-arch\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repo\cell-arch\.planning\PROJECT.md
@C:\Repo\cell-arch\.planning\REQUIREMENTS.md
@C:\Repo\cell-arch\.planning\phases\05-ci-cd-security\05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSI Secret Store driver manifest</name>
  <files>infra/secret-store/secretstore-deployment.yaml</files>
  <action>
    Create a Deployment and associated ServiceAccount that installs the CSI driver (github.com/kubernetes-sigs/secrets-store-csi-driver).  
    Set the driver to use `aws-secretsmanager` and `azure-keyvault` providers, referencing the IAM role and federated credential (per D-15). No secrets are hard-coded.  
  </action>
  <verify>
    <automated>kubectl apply --dry-run=client -f infra/secret-store/secretstore-deployment.yaml</automated>
  </verify>
  <done>YAML file validates and contains provider configurations.</done>
</task>

<task type="auto">
  <name>Task 2: Add cert-manager ClusterIssuer manifest</name>
  <files>infra/cert-manager/clusterissuer.yaml</files>
  <action>
    Create a `ClusterIssuer` that uses **self-signed** for internal services (or ACME with a test CA).  
    Annotate with `cert-manager.io/issuer-name: internal-ca`.  
    Ensure it is scoped to the namespaces used by the sidecar.  
  </action>
  <verify>
    <automated>kubectl apply --dry-run=client -f infra/cert-manager/clusterissuer.yaml</automated>
  </verify>
  <done>ClusterIssuer yaml passes dry-run and references no static certificates.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| CSI → Cloud secret stores | OIDC-federated credentials only. |
| cert-manager → TLS issuance | No private keys stored in repo. |
## STRIDE
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-10 | Spoofing | CSI provider config | mitigate | IAM role trust policy restricts `sts:AssumeRoleWithWebIdentity`. |
| T-05-11 | Information Leakage | ClusterIssuer yaml | mitigate | No private key data; use `spec.privateKeySecretRef` pointing to a K8s secret created at runtime. |
</threat_model>

<verification>
Run `kubectl apply -f` in a kind cluster (or test cluster) and verify the driver pods become Ready and the ClusterIssuer reports `Ready`.  
Check that `kubectl get secretstore` shows no secret data.  
</verification>

<success_criteria>
CSI driver and cert-manager are successfully deployed and ready; sidecar can later request secrets via the CSI volume.  
</success_criteria>

<output>
Create `.planning/phases/05-ci-cd-security/05-05-PLAN.md`
</output>
