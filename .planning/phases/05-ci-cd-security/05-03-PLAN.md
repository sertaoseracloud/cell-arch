---
phase: "05-ci-cd-security"
plan: "03"
type: execute
wave: 2
depends_on: ["05-01-PLAN"]
files_modified:
  - ".github/workflows/integration-test.yml"
requirements: ["CICD-04","SECR-01"]
autonomous: true
must_haves:
  truths:
    - "Integration tests run in CI using testcontainers-go"
  artifacts:
    - path: ".github/workflows/integration-test.yml"
      provides: "Integration-test workflow"
  key_links:
    - from: "integration-test.yml"
      to: "testcontainers-go"
      via: "docker run"
---

<objective>
Add a CI job that runs the Go integration test suite against LocalStack (AWS) and CosmosDB emulator (Azure).  
Purpose: Validate sidecar behaviour in a realistic but isolated environment, per D-14.  
Output: `.github/workflows/integration-test.yml`
</objective>

<execution_context>
@C:\Repo\cell-arch\.claude\get-shit-done\workflows\execute-plan.md
@C:\Repo\cell-arch\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repo\cell-arch\.planning\PROJECT.md
@C:\Repo\cell-arch\.planning\REQUIREMENTS.md
@C:\Repo\cell-arch\.planning\phases\05-ci-cd-security\05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold integration-test workflow</name>
  <files>.github/workflows/integration-test.yml</files>
  <action>
    Create a workflow that:
    1. Runs on `push` and `pull_request` after the `ci.yml` and `scan.yml` jobs succeed (`needs: [ci, scan]`).
    2. Sets up Docker (actions/setup-docker) and pulls `localstack/localstack` and `mcr.microsoft.com/cosmosdb/linux/azure-cosmos-emulator`.
    3. Starts both containers in the background.
    4. Executes `go test ./test/integration/... -run TestIntegration -count=1`.
    5. Uses OIDC for any needed temporary cloud credentials (no static keys).  
    Ensure the containers are stopped after the job.
  </action>
  <verify>
    <automated>git diff --exit-code .github/workflows/integration-test.yml</automated>
  </verify>
  <done>Workflow file exists, lint-free, and can be triggered manually.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| CI → Docker containers | Containers run in isolated environment; no network exposure. |
## STRIDE
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-06 | Spoofing | Docker image tags | mitigate | Pin exact image digests for LocalStack and Cosmos emulator. |
| T-05-07 | Denial-of-Service | Test containers | mitigate | Limit container memory/CPU via `services.<name>.options`. |
</threat_model>

<verification>
Run `gh workflow run integration-test.yml` and verify the job ends with a green check.  
Check that no secret values appear in the log (search for `[A-Z0-9]{40}`).  
</verification>

<success_criteria>
Integration-test job passes on the first run and reports zero failures.  
</success_criteria>

<output>
Create `.planning/phases/05-ci-cd-security/05-03-PLAN.md`
</output>
