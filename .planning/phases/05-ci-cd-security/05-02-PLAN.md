---
phase: "05-ci-cd-security"
plan: "02"
type: execute
wave: 1
depends_on: ["05-01-PLAN"]
files_modified:
  - ".github/workflows/scan.yml"
requirements: ["CICD-02","CICD-03","SECR-01"]
autonomous: true
must_haves:
  truths:
    - "Trivy container scan runs on built images"
    - "tfsec scans Terraform configuration"
  artifacts:
    - path: ".github/workflows/scan.yml"
      provides: "Scanning workflow definition"
  key_links:
    - from: "scan.yml"
      to: "Trivy"
      via: "trivy image"
    - from: "scan.yml"
      to: "tfsec"
      via: "tfsec ."
---

<objective>
Add security-scanning jobs (Trivy, tfsec, govulncheck) to the pipeline.  
Purpose: Detect vulnerabilities early, fulfilling the locked scanning decision (D-12).  
Output: `.github/workflows/scan.yml`
</objective>

<execution_context>
@C:\Repo\cell-arch\.claude\get-shit-done\workflows\execute-plan.md
@C:\Repo\cell-arch\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repo\cell-arch\.planning\PROJECT.md
@C:\Repo\cell-arch\.planning\REQUIREMENTS.md
@C:\Repo\cell-arch\.planning\phases\05-ci-cd-security\05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scan workflow</name>
  <files>.github/workflows/scan.yml</files>
  <action>
    Define a workflow triggered after `ci.yml` succeeds (`needs: build`).  
    Jobs:
    * `trivy-scan` – uses `aquasecurity/trivy-action@master` to scan the Docker image built in `ci.yml`.
    * `tfsec-scan` – runs `aquasecurity/tfsec-action@master` on the `infra/` directory.
    * `govulncheck-scan` – runs `golangci/govulncheck-action@v1` on the Go modules.
    All jobs run with OIDC (`permissions.id-token: write`) per D-11 and **must not** reference any secret values (fulfils D-10).  
  </action>
  <verify>
    <automated>git diff --exit-code .github/workflows/scan.yml</automated>
  </verify>
  <done>Scanning workflow file exists with three jobs and passes `actionlint`.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Scan → Container registry | Trivy reads image layers; no credentials needed. |
| Scan → Terraform files | tfsec reads plain text; no secrets exposed. |
## STRIDE
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-04 | Tampering | scan.yml | mitigate | Branch protection & required status checks (D-13). |
| T-05-05 | Information Leakage | Scan output logs | mitigate | Configure actions to mask any discovered secrets (`::add-mask`). |
</threat_model>

<verification>
Trigger a manual workflow run via `gh workflow run scan.yml`.  
Confirm that each job completes with a "PASS" status.  
</verification>

<success_criteria>
All three scanning jobs execute without secret leakage and fail the pipeline on any high-severity finding.  
</success_criteria>

<output>
Create `.planning/phases/05-ci-cd-security/05-02-PLAN.md`
</output>
